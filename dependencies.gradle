ext {
    // App dependencies
    androidSupportVersion = '25.1.1'
    supersimpleVersion = '0.1.7'

    // Kotlin
    kotlinVersion = '1.0.6'

    // RxJava
    rxJavaVersion = '2.0.5'
    rxAndroidVersion = '2.0.1'

    // Dagger2
    daggerVersion = '2.9'

    // Requery
    requeryVersion = '1.1.2'

    // GSON
    gsonVersion = '2.8.0'

    // OkHttp + Retrofit2
    okHttpVersion = '3.6.0'
    retrofit2Version = '2.1.0'
    retrofit2RxJava2Version = '1.0.0'

    // Picasso
    picassoVersion = '2.5.2'

    // test
    androidSupportTest = '0.4.1'
    espressoTestVersion = '2.2.1'
    junitVersion = '4.12'
    robolectricVersion = '3.2.2'
    mockitoVersion = '2.7.1'
    mockitoKotlinVersion = '1.2.0'
}
